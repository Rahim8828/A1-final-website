import React from 'react';
import { Link } from 'react-router-dom';
import { Calendar } from 'lucide-react';

const StickyBookButton: React.FC = () => {
  return null; // Hidden as per requirement
};

export default StickyBookButton;
